import { OfficialfilterPipe } from './officialfilter.pipe';

describe('OfficialfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OfficialfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
