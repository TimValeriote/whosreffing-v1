import { DecimalpercentpipePipe } from './decimalpercentpipe.pipe';

describe('DecimalpercentpipePipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalpercentpipePipe();
    expect(pipe).toBeTruthy();
  });
});
